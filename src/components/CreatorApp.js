import React from 'react';

export default class ArtistApp extends React.Component {
    render() {
        return (
            <div>
                This is a placeholder (artist).
            </div>
        );
    }
}